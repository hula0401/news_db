## authorized edits:
1. ask when need to do rf-m

## Documentation
1. for each fix, will record all in one MD file "docs/FIX_Database_Docs.md".
2. for each claude session/edition, don't generate extra solo md files. Record everything in "docs/RECORD_Change.md".
3. For all documentation. Keep words precise. Each addition of record will add a one-sentence index indicated with edit time.
4. Documentation should be clear and less redundant that minimizes repeated contents.
